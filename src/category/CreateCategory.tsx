import { Typography } from "@mui/material"
import { Box } from "@mui/system"

export const CreateCategory = () => {
  return (
    <Box>
        <Typography variant="h1">Create Category/ subCategory</Typography>
    </Box>
  )
}
